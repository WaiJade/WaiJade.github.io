---
title: "MDX 语法与样式测试"
description: "用于集中测试当前博客支持的 Markdown、MDX 与扩展指令样式。"
pubDate: "2026-03-15"
author: "WaiJade"
tags:
  - "测试"
  - "MDX"
  - "样式"
draft: false
featured: false
---

这篇文章专门用来回归测试文章渲染效果。这里会同时覆盖 Markdown、GFM、数学公式、原生 HTML 对齐块，以及新接入的指令语法，比如 :highlight[行内高亮]{color="#f3d36b"}。

## 标题层级

### 三级标题

#### 四级标题

---

## 段落与行内样式

这是一个普通段落，包含 **加粗文本**、*斜体文本*、***加粗斜体***、~~删除线~~ 和 `行内代码`。

也可以测试链接样式：[返回首页](/) 和 [关于页](/about/)。

## 列表

- 无序列表第一项
- 无序列表第二项
  - 子项 2.1
  - 子项 2.2
- 无序列表第三项

1. 有序列表第一项
2. 有序列表第二项
   1. 子项 2.1
   2. 子项 2.2
3. 有序列表第三项

## 任务列表

- [x] 已完成的样式检查
- [ ] 待确认移动端样式
- [ ] 待补充更多自定义语法

## 引用

> 这是一级引用。
>
> 第二段引用用来测试段落间距。

## Callout 指令

:::danger{title="怪怪的错误框" color="#5ea87a"}
这里是一个故意覆盖颜色的 `danger` 指令，用来验证自定义标题和颜色变量是否正常生效。
:::

:::success{title="成功提示"}
这里是成功提示块，适合放构建成功、任务完成或者状态确认。
:::

:::info
这里是信息提示块，适合放补充说明或上下文提示。
:::

:::note
这里是注意事项，适合强调阅读条件、兼容性或迁移背景。
:::

:::tip
这里是提示块，可以放写作建议、快捷技巧或者延伸阅读。
:::

## 表格

| 字段 | 值 | 说明 |
| ---- | ---- | ---- |
| `title` | 字符串 | 文章标题 |
| `pubDate` | 日期 | 文章发布时间 |
| `tags` | 数组 | 标签集合 |

## 代码块

```ts
type PostMeta = {
  title: string;
  pubDate: string;
  tags: string[];
};

export function formatMeta(meta: PostMeta) {
  return `${meta.title} - ${meta.pubDate} - ${meta.tags.join(", ")}`;
}
```

```bash
npm run build
npm run check
```

```json
{
  "title": "MDX 语法与样式测试",
  "draft": false,
  "tags": ["测试", "MDX", "样式"]
}
```

## 数学公式

行内公式示例：$e^{i\pi} + 1 = 0$。

块级公式示例：

$$
\int_{0}^{1} x^2 \, dx = \frac{1}{3}
$$

## 图片

![站点头像测试图](/avatar.png)

## 原生 HTML 对齐块

<div align="center">

风吹山影动  
夜色慢慢落进杯中  
如果这一段还是忽紧忽松  
就继续拿它当行距测试样本  
</div>

<p align="right">（2026年3月15日 23:40）</p>

## 混合内容

最后再放一段混合内容，确认 `:highlight[高亮文字]`、`inline code`、[普通链接](/archives/) 和列表不会互相打架：

- 第一项里包含 :highlight[重点词]{color="var(--accent)"}。
- 第二项里包含 `const answer = 42`。
- 第三项里包含一个链接到 [归档页](/archives/)。
