# 博客重构迁移计划

## 1. 背景与目标
- 当前仓库是基于旧 Jekyll 模板维护的个人博客，内容可继续使用，但模板结构、视觉语言和后续扩展方式已不符合新的需求。
- 新站目标是纯静态个人博客，不引入后端，源码继续放在 GitHub，部署到 GitHub Pages 或 Cloudflare Pages。
- 新技术栈固定为 `Astro + React + TypeScript + Tailwind + Radix UI + MDX`。
- 重构结果应同时完成两件事：
  - 从旧 Jekyll 结构迁移到新的 Astro 结构。
  - 从旧模板式博客升级为带明确视觉系统的个人博客站。

## 2. 总体原则
- 先搭新骨架，再迁内容，再补视觉，再补功能，不反过来做。
- 保留“文章文件夹 + 集中配置”的工作方式，但升级为 Astro Content Collections。
- 不做自建后台，不做数据库，不做服务端接口；所有页面都由静态构建生成。
- 视觉可参考 `/home/WaiJade/Documents/AstroBox/AstroBoxWebsite` 与 `/home/WaiJade/Documents/AstroBox/AstroBox-NG/web` 的气质，但页面职责仍以个人博客为中心，不做成产品官网或应用入口。

## 3. 目标架构

### 3.1 目录结构
```text
src/
  components/
  layouts/
  pages/
  content/
    posts/
    pages/
    notes/
    config.ts
  config/
    site.ts
  styles/
  lib/
public/
```

### 3.2 内容组织
- `src/content/posts/`：正式文章，主站核心内容。
- `src/content/pages/`：关于页、朋友页、声明页等单页内容。
- `src/content/notes/`：可选，用于短文、随笔、碎片记录。
- `src/content/config.ts`：定义 collection schema，统一 frontmatter 字段、类型校验和默认值。
- `src/config/site.ts`：集中管理站点标题、副标题、导航、社交链接、友情链接、首页文案、主题配置等全站级设置。

### 3.3 页面体系
- 首页：品牌化 Hero、最新文章、精选专题、标签入口、个人简介。
- 文章详情页：标题、日期、标签、目录、封面、正文、上下篇、相关推荐。
- 归档页：按年份或时间线浏览。
- 标签页：按标签筛选文章。
- 关于页 / 朋友页 / 项目页：继续静态化。
- 404 页：与全站视觉一致，不再沿用默认模板感。

## 4. 迁移阶段

### 阶段一：搭建新工程
- 初始化 Astro 项目并接入 React、TypeScript、Tailwind、MDX、Radix UI。
- 建立 `src/content/config.ts` 与 `src/config/site.ts`。
- 建立基础布局、全局样式、主题变量、字体策略和 SEO 基础配置。
- 先不迁内容，先把空壳站跑通。

### 阶段二：定义内容模型
- 从旧 `_posts/` 中提炼统一的 frontmatter 字段，例如：
  - `title`
  - `description`
  - `pubDate`
  - `updatedDate`
  - `tags`
  - `cover`
  - `draft`
  - `series`
  - `featured`
- 确定 slug 规则，避免迁移后链接混乱。
- 设计 `posts`、`pages`、`notes` 三类内容集合，不把所有内容混在一个目录。

### 阶段三：迁移旧内容
- 旧文章从 `_posts/` 迁移到 `src/content/posts/`，优先保证正文和元数据保真。
- 单页内容从旧模板页面拆分到 `src/content/pages/` 或 Astro 页面。
- 旧图片资源先保留，后续再统一整理到更合理的静态资源目录。
- 迁移过程中不立即删除旧 Jekyll 文件，直到新站覆盖相同内容。

### 阶段四：建立页面骨架
- 首页、文章详情页、归档页、标签页、关于页、404 页全部用 Astro 重新实现。
- 页面逻辑以静态渲染为主，只有局部交互才用 React 组件。
- React 组件主要承担：
  - 主题切换
  - 抽屉 / 对话框
  - 目录高亮
  - 局部动画或阅读辅助交互

### 阶段五：建立视觉系统
- 视觉方向参考 AstroBox 风格中的以下元素：
  - 大面积留白
  - 模糊渐变背景
  - 半透明或轻玻璃卡片
  - 清晰的主次排版
  - 品牌化首页 Hero
- 视觉落地时必须做博客化转译：
  - 首页不是应用控制台，而是“个人介绍 + 内容入口”。
  - 卡片不是资源商城卡片，而是文章、专题、标签、项目等内容卡片。
  - 动效服务于阅读节奏，不堆叠炫技效果。
- 应先定义全局设计令牌：
  - 颜色
  - 间距
  - 圆角
  - 边框透明度
  - 阴影层级
  - 标题与正文字体

### 阶段六：补齐静态功能
- 文章目录与代码高亮。
- 标签、归档、RSS、sitemap。
- 静态搜索。
- 静态友好的评论方案。
- 图片、封面、社交卡片和元信息优化。

### 阶段七：下线旧模板
- 当新站已覆盖首页、文章页、归档、标签、关于等核心页面后，再开始清理旧 Jekyll 结构。
- 删除旧模板前先确认：
  - 关键文章已迁移完成
  - 旧链接有替代方案或重定向策略
  - 静态部署已通过验证

## 5. 界面方向

### 5.1 首页
- 顶部应为强识别 Hero，而不是普通博客横幅。
- 首页内容顺序推荐为：
  - 个人标题区
  - 一段明确的自我介绍
  - 最新文章
  - 精选专题或置顶文章
  - 标签或栏目入口
  - 友情链接 / 页脚

### 5.2 文章页
- 文章页重点是阅读体验，不要做得像文档站或应用详情页。
- 需要清晰处理：
  - 标题层级
  - 元信息
  - 正文宽度
  - 图片展示
  - 代码块
  - 引注 / 提示块
  - 目录与上一篇下一篇

### 5.3 归档与标签
- 归档页强调时间感，标签页强调主题感。
- 视觉上延续首页的设计语言，但密度更高、交互更轻。

## 6. 配置与内容工作流
- 编辑文章时，继续采用“把 `.md` 或 `.mdx` 放入对应文件夹”的方式。
- 所有站点级配置集中到单一配置文件或极少数配置文件中，避免配置散落。
- 推荐长期工作流：
  - 写文章：`src/content/posts/*.mdx`
  - 改站点信息：`src/config/site.ts`
  - 改内容模型：`src/content/config.ts`
  - 改视觉组件：`src/components/` 与 `src/layouts/`
- 这种工作流与新架构完全兼容，而且比旧 Jekyll 模式更适合后续扩展。

## 7. 验收标准
- 新站能在纯静态模式下完成构建与部署。
- 核心页面已由 Astro 体系接管，不再依赖旧 Jekyll 模板。
- 旧文章已迁移到新的内容集合体系，frontmatter 结构统一。
- 全站视觉语言统一，首页、文章页、归档页和关于页属于同一设计系统。
- 后续协作者看到仓库时，能够通过本计划文档理解目标结构、迁移顺序和视觉方向。

## 8. 执行约束
- 后续所有重构任务都应先对照本计划文档，再决定修改路径。
- 若当前仓库中的旧结构与本计划文档冲突，应视为迁移中遗留，而不是新的长期标准。
- 在重构完成前，旧 Jekyll 文件只做必要维护，不再扩展其模板体系。
